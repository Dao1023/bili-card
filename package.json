{
  "name": "bili-card",
  "version": "0.5.2",
  "description": "Render one-line bilibili video/UP cards as card galleries in Obsidian",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs"
  },
  "devDependencies": {
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.0"
  }
}
